import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.buycrypt.app',
  appName: 'BuyCrypt',
  webDir: 'dist/crypto-app/browser',
 
};

export default config;